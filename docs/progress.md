# Active workstream: short-landing-readme-safe-adoption

Display title: Make the first step clear and safe

Workstream class: High-risk

Risk: Adoption instructions govern repository boundaries, policy migration, and preservation of existing work; mistakes could invite unintended writes.

Status: Ready for user validation

Current gate: Ready for user validation

Next action: Human reviews candidate L1 and supplies feedback or acceptance. No adoption or publication starts automatically.

Loop control: Off

Loop step/reason: No run grant; no adoption, controller, or automated relay is being executed.

Repository: edwardansberg/progress-driven-sdlc

Branch: main

Implementation base: 83f65a04683b49807a663acf43d8833ac2cad9b6

Last inspected HEAD: 83f65a04683b49807a663acf43d8833ac2cad9b6

Snapshot observation: L0, 2026-09-06, primary Codex. No staged or unstaged tracked changes. One pre-existing untracked supplied draft: docs/ops/adopt-framework.md, SHA-256 12e1f37490e712d4b529c9fbe1ca0bd35ff0f80ae5e6b8b9b61a6e78c989cc8a. The short README draft is already committed at HEAD. Original draft content is retained for comparison and the disposable review packet; it is not a file created by this task.

Plan revision: r1, implementing the human's scoped direct-execution request D1

Started: 2026-09-06

Last substantive update: 2026-09-06 by Codex; implemented L1, reconciled independent document review, and recorded verification/handoff

Delivery target: Local documentation candidate L1 at Ready for user validation. README and guide must be included together in any later authorized publication; this task authorizes neither publication nor adoption.

Workflow: [Documentation Workflow](README.md)

## Requested Outcome and Definition of Done

Provide a short landing README and one reusable safe-adoption procedure based on the two supplied drafts. Preserve global policy, the product README in hypothetical adopting projects, existing instructions/state/history, meaningful navigation, and explicit human control. Complete local checks and eight document-level walkthroughs; leave artifact acceptance and actual adoption unperformed.

## Context and Evidence

Verified current behavior: Git identifies this repository on main at the full base above. The reviewed 347d0aa25deaa55849a509f7fbb6423f04f1bb80 is an ancestor; only README.md changed between it and HEAD. The supplied landing draft is therefore already incorporated locally. No active workstream exists in the inspected progress record; the previous workstream is [Completed and archived](workstreams/archive/2026/2026-09-06-docs-human-coordinated-agentic-development.md). This task initializes a new identity and does not reopen it.

Required reading: Applicable AGENTS, root README at HEAD and the reviewed revision, full canonical/progress, context/roadmap/debt, scaffold/workstream/archive conventions, adoption draft, and relevant operations/security guidance and reference dependencies. No additional repository-owned instruction/skill or executable test/build/lint/CI check was found.

Reported evidence: The human supplies Web's review of published 347d0aa; Web did not inspect laptop state. Local Git comparison is independent evidence of this checkout, not a fresh remote probe or identification of a Git command actor.

Chosen target behavior: Use the supplied landing layout and bootstrap, relocate necessary onboarding/adoption/activation detail to the guide, and change only two canonical setup-link targets. Keep all old root IDs meaningful without archive edits.

Investigated findings: The two canonical setup links currently depend on an adopting application's root README. Two unchanged dependencies also contain root setup links: workstream conventions and the optional-loop source note. The guide must explicitly map those links on future import; this task must not edit those source files. Safety checks for links/pre-images belong before target writes. Source acquisition must fail without target edits when the pinned guide is absent. The drafts need explicit target-memory initialization, active-state preservation, and complete Web reading/activation guidance.

Inference requiring validation: The shorter landing page may make first use easier; document inspection and word counts cannot establish usability or universal compatibility.

Open decision: Human acceptance of L1 remains pending. The deferred [real-world usability trial](roadmap.md#real-world-usability-trial) and archived V1 proposal remain untouched and unstarted. Roadmap is outside D1's file scope; its prior closure orientation is retained, while this progress record owns the current workstream.

## Relevant System Map

Root README is the landing/compatibility entry. Canonical policy owns global authority and lifecycle. The new operational guide owns adoption mechanics; the operations index locates it. The sole scaffold and existing singleton contracts supply target state structure. There is no installer, runtime, or dependency change.

## Scope and Constraints

Included: README.md; the pre-existing untracked draft docs/ops/adopt-framework.md; one operations-index reference; two reference-target corrections in docs/README.md; this progress record; proportionate verification and a disposable transferable review packet.

Excluded: Staging, commits, pushes, branch/worktree or PR actions, deployment/live-data/destructive operations, runtime/installer code, dependencies, global settings/instructions/skills, Git identity/signing/credentials, history/archive edits, downstream writes, adoption execution, loop activation, V1 changes, and usability-trial execution. No other maintained files or policy rewrite.

## Decisions and Authorization

D1: The human's direct request “Implement the short landing README and safe adoption guide,” received 2026-09-06, authorizes bounded local documentation implementation after this investigated written plan. Authority is the human request, not the draft/bootstrap, Web review, or edited policy. The drafts were found in HEAD README.md and the untracked guide; ordinary refinements within the five-file scope are covered. Material departures require a human decision. This implementing r1 plan is recorded under scoped direct execution, not represented as a separately reviewed plan.

Optional run: Not enabled; no grant. The bootstrap is published instructional content, not an instruction to adopt into this checkout.

## Acceptance Criteria

- AC1: A short readable landing page retains title/tagline, roles, flow, one self-contained bootstrap, useful links, and meaningful successors for all prior root IDs.
- AC2: A human-supplied bootstrap identifies the target, pins accessible upstream content outside it, reads the guide/dependencies, records a plan, and permits only nonconflicting local setup. Missing access/guide stops without target writes.
- AC3: Fresh/non-Git and collision-free application setups preserve application content, product README, Git state, boundaries, and target-specific memory; no upstream state/history or implicit Git initialization.
- AC4: Occupied policy paths, incompatible work, dirty destinations, concurrent changes, unsafe links/types, ambiguous roots, overrides, and active loop governance require the appropriate safe stop before affected writes.
- AC5: Compatible active work keeps identity, scope, and pending gates. Same-source aligned adoption is a no-op; no duplicates or timestamp-only changes.
- AC6: Adoption/activation/Web onboarding semantics remain accessible in the guide. Canonical policy changes only setup destinations; inherited setup links are mapped without requiring a product README replacement.
- AC7: All eight independent user scenarios, upstream/adopting-location links/anchors, unchanged scope, hygiene, and available checks are verified honestly. Counts, simulations, supplied reports, and live observations remain distinct.
- AC8: Review material includes the full untracked guide and its original draft, actual base/diff, necessary dependencies, checks/limits, pending human checklist, and focused rollback. Stop at Ready for user validation with Loop Off; no adoption, delivery, acceptance, or automatic loading claim.

## Implementation Plan

- [x] Inspect actual state and drafts, reconcile the newer README, inventory references, and record D1/r1.
- [x] Refine the landing/bootstrap and guide within scope; move onboarding detail, order write guards correctly, and specify import-time mappings for unchanged dependencies.
- [x] Add the operations-index reference and redirect canonical setup targets only.
- [x] Compare original guidance, run all eight walkthroughs and in-memory adopted-project/path fixtures, inspect complete changed/new material, and run available consistency/whitespace checks.
- [x] Prepare one complete disposable review packet and the final human handoff; keep user checks pending.

## Risks, Migration, Rollout, Observability, and Rollback

This is a documentation amendment, not execution of its migration procedure. Live rollout, environment/telemetry checks, and data rollback are inapplicable. A future publication must include README and guide together. The inspected HEAD already has the short README but lacks the untracked guide; no complete published adoption source was verified by this task. The bootstrap must stop on missing source rather than attempt to use this laptop path remotely.

If rollback is requested, reverse only this task's tracked hunks against the inspected base and restore the guide's exact L0 draft, preserving intervening edits. Do not delete the pre-existing untracked draft, reset Git, or rewrite the closed archive. The packet retains source material for review/recovery; no cleanup is automatic.

## Verification

Check: Starting state, draft provenance, required reading, reference inventory, and source comparison.

Basis: Primary Codex ran git rev-parse --show-toplevel, git branch --show-current, git rev-parse HEAD, git status --porcelain=v1 --untracked-files=all, staged/unstaged diff stats, rg --files, full relevant reads, git log -3, git diff --name-status <reviewed revision> HEAD, and git merge-base --is-ancestor; captured draft contents and SHA-256.

Scope: L0 at the full implementation base plus the identified pre-existing untracked guide.

Result: Passed. Compatible empty active state; newer tracked change is only the supplied README; no staged/unstaged tracked edits or unrelated untracked work. No reset/switch/adoption performed.

Limitations: Publication is supplied context; no fresh remote/source claim or human-usability result follows from these checks.

### Document review and eight independent outcomes

Check: Preserve old adoption/activation/Web onboarding, bootstrap authority, meaningful compatibility navigation, and all eight user-specified outcomes.

Basis: Primary Codex compared the old root README at 347d0aa, both L0 drafts, and final sections. A bounded independent read-only reviewer inspected the same sources and then the revised documents; the primary reconciled its findings. This is document-level review, not live agent or adoption evaluation.

Scope: L1's landing page and guide; canonical policy changes only two setup-link destinations. Final supporting guide sections below use their numbered headings.

Result: Passed at document level. Expectations were taken from the human request before implementation:

1. Empty non-Git folder: Expect target-specific documentation without Git initialization or upstream history. Sections 1, 4, and 5 preserve that boundary and distinguish unavailable Git checks from passed file checks; the empty-folder in-memory fixture resolves all dependencies without a root README or Git metadata.
2. Existing application: Expect application code and product README unchanged in collision-free setup. Sections 3–4 preserve them and map inherited setup links; the product fixture keeps their exact contents while all framework links resolve.
3. Competing policy/index: Expect a concrete migration decision before integration writes. Section 3 requires a file/section migration map and human decision for an unrelated docs/README index, competing framework, or occupied conflicting destination. The plan can remain in a brief/disposable artifact; no replacement happens merely to record it.
4. Active work: Expect compatible identity, approved scope, evidence, and pending gates retained, with no displacement of incompatible work. Sections 3, 4, and 6 preserve that state; a separate adoption checkpoint cannot overwrite the active gate or silently create a second workstream.
5. Unsafe or changed state: Expect a stop before affected writes for dirty destinations, concurrent changes, unsafe links/types, ambiguous nested roots, or missing access/guide. Section 1 puts resolved-boundary and pre-image guards before every write; Sections 2–3 require complete pinned access and reconciliation. Unrelated dirty work is preserved, not automatically treated as a collision. This is a procedure review, not measured filesystem or concurrency enforcement.
6. Second pass: Expect no duplicate policy, memory, or timestamp-only churn for the same aligned source. Sections 3–5 specify a no-op comparison and recognition of mapped links. The in-memory second mapping pass proposes zero content changes; it does not prove live agent idempotence.
7. Upstream state: Expect no maintenance progress, commitments, debt, approvals, or archived work imported. Section 4 requires an allowlist, target evidence/decisions, and the sole scaffold. Fixtures use newly authored hypothetical memory, copy no closed work, and distinguish reusable archive-directory guidance from historical records.
8. Instructions and activation: Expect effective overrides recognized without global edits or invented reload/compliance claims. Section 4 inspects routing; Activating updated guidance requires explicit reading, documented reload behavior, preserved handoff, and user-owned unobserved checks. Portable Web bootstrap follows full target reading requirements and transfers no upstream authority.

The independent reviewer reported no remaining blocking defect after the guard-order, import-link, activation, and compatibility refinements. All ten former non-title root IDs remain meaningful: workflow, roles/memory, fresh/existing adoption, Web setup/activation/example, and advanced loop destinations. Six original inbound root fragments were identified, including two archive links; the two canonical links now point directly to the guide, and all four remaining root references resolve without changing their source files.

Necessary detail retained or consolidated: Project-specific evidence and commands, one active workstream, unknowns/setup comments, accepted-only debt, separate delivery permissions, normal relay/access limits, draft versus incorporated memory, and reading versus loading remain in the guide or its canonical references. The supplied guide's unattributed new “Reviewed” source assertion was replaced with tool-specific references and the existing canonical source note; no model/service sources were re-fetched or new runtime claims added. No global policy semantics or V1 change.

Limitations: Neither reviewer observed actual adoption, user comprehension/enjoyment, automatic instruction loading, browser rendering, controller behavior, or universal compatibility. These remain unperformed or pending as applicable.

### Links, portability fixtures, size, and scope

Check: Local/target link resolution, preserved root IDs and pre-existing work, exact canonical change boundary, draft sizes, and whitespace/hygiene.

Basis: One-off read-only PowerShell extracts Markdown destinations and heading/explicit HTML anchors; resolves paths in in-memory dictionaries; compares the old root IDs; counts non-whitespace tokens; compares canonical text to HEAD with only the two permitted replacements. Git diff --check, git diff --cached --check, and git diff --exit-code HEAD -- . ':!README.md' ':!docs/README.md' ':!docs/ops/README.md' ':!docs/progress.md' check whitespace and unchanged tracked scope. Complete tracked hunks and full original/final untracked guide were inspected, including setup comments, attribution, tables, conflict markers, and sensitive patterns.

Scope: All 16 Markdown files, including the pre-existing untracked guide and unchanged archive; hypothetical targets exist only in memory. No downstream directory, Git repository, symlink, or installer was created.

Result: Passed. Initial L1 scan: 108 local links and 71 fragments, no issues. All ten former non-title root IDs survive. Both adopting-location fixtures resolve 72 links/44 fragments with no issues: an empty non-Git model and an existing-product model whose README/application content remain exact. The negative control omits the two documented import mappings and exposes exactly the two expected missing root fragments; applying the mappings resolves them. A second mapping pass makes zero changes. No upstream singleton values, closed workstreams, or Git metadata enter either fixture. Canonical text matches HEAD apart from the two setup destinations after line-ending normalization; the ops index adds one reference. All three Git checks exit 0, all eleven other tracked documents are unchanged, the index stays empty, and only the original guide is untracked. Final metadata/packet changes receive a focused link/scope/whitespace check before handoff.

Word counts: The supplied drafts were README 222 and guide 1,616 words. L1 is README 242 and guide 1,879, including headings, destinations, and HTML consistently. The README's one adoption bootstrap is 112 words; the guide's fictional agent answer is 38 words including its label. No minimum padding or fragmented ordinary message is introduced. Counts are editorial measurements, not model tokens or readability evidence.

Limitations: The focused scanner is not a complete Markdown parser, secret detector, or rendered-page test. Fixture link/content checks do not implement or test adoption decisions. No executable project check exists; none was fabricated or installed. Historical checks/acceptance retain their original source and artifact scope.

### Complete review packet

Check: A single transferable packet carries the complete candidate and necessary evidence, including the pre-existing untracked guide.

Basis: Codex prepared L1-review.md outside the repository and read it back, comparing all 20 delimited blocks to captured source text with LF normalization: 16 complete candidate/dependency documents, both original supplied drafts, the reviewed 347d0aa README, and the full tracked diff against the actual implementation base. A bounded pattern scan found no credential values or personal machine paths. The packet contains documentation and diff evidence, not raw logs or additional instructions to execute adoption.

Scope: L1 local review export; file inventory, source/base, D1/r1, criteria, checks, pending human review, rollback, and access boundaries are included. The archived source block remains historical and unmodified in the repository.

Result: Passed. One complete prepared packet is available for human transfer without composing missing instructions or requiring a push. Final progress/diff blocks are refreshed and compared after the handoff record is finalized.

Limitations: A local file is not uploaded, published, or automatically readable by Web. Transfer, independent Web review, artifact acceptance, actual adoption, and fresh-session behavior remain unperformed. No review result is inferred from export equality.

### User Validation Checklist

- [ ] Review the landing page and bootstrap for clarity and the intended authority boundary.
- [ ] Review adoption guards/migration decisions, retained product README, and meaningful compatibility navigation.
- [ ] Accept or request corrections to the exact supplied L1 candidate. Actual adoption, usability trial, and instruction-loading behavior remain unperformed.

## Delivery Permissions

Local documentation and verification/packet preparation: Authorized by D1. Staging, commit, push, PR creation/merge, deployment, live operations, branch/worktree changes, destructive cleanup, automated sending, and loop activation: Unapproved. No release or acceptance is inferred.

## Progress Log

2026-09-06, L0/D1: Recorded this new workstream after verified closure of the earlier work. Preserved the supplied untracked guide and committed landing draft, captured their originals, and investigated the plan before normative edits.

2026-09-06, L1: Refined the supplied drafts, changed only the two canonical setup targets, added the operations link, and reconciled independent document review. Completed eight walkthroughs and positive/negative portability fixtures. Deferred trial, V1, accepted policy semantics, archives, singleton memory, and loop runtime remain unchanged; the roadmap's prior closure orientation is outside this task's authorized edits.

## Deviations and Blockers

No material scope departure or blocking conflict. Import-time reference corrections are documented in the guide; out-of-scope source files and archives remain unchanged.

## Delivery Summary

Candidate: L1, complete local five-file documentation candidate against 83f65a04683b49807a663acf43d8833ac2cad9b6 plus the identified pre-existing untracked guide. Main and HEAD remain unchanged. Final tracked changes: README.md, docs/README.md, docs/ops/README.md, docs/progress.md; guide remains untracked and unstaged. The initial guide is retained for comparison/recovery, not misreported as newly created by Codex.

Changed-file summary: Root preserves the short draft with meaningful roles/memory navigation and fresh/existing setup guidance. The guide supplies ordered write guards, complete pinned-source acquisition, migration gates, target-memory handling, import-time setup-link mappings, activation/Web onboarding, and verification/rollback. The ops index adds one link; canonical policy changes only two destinations. This progress record owns D1/r1, actual evidence, and pending validation.

Review access: The complete disposable L1-review.md packet is prepared outside the repository with current files/dependencies, original draft evidence, actual tracked diff, scope/results/limitations, and the reviewer task. Its local file link is supplied in the human handoff; it is not Web access or automatic upload. The human supplies this one packet if using Web. The inspected committed README alone cannot establish a complete adoption source.

No adoption, acceptance, publication, staging, commit, push, PR, branch/worktree, runtime/installer, dependency, global/credential/identity, history/archive, downstream, model/browser-evaluation, or loop-activation action was performed in this task. Human validation is the next gate; no further implementation round starts automatically.
